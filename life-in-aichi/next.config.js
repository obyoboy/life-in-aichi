/** @type {import('next').NextConfig} */
const nextConfig = {
  // Multi-language routing via [lang] dynamic segment
  // No i18n config needed with App Router — we handle it via folder structure
};

module.exports = nextConfig;
